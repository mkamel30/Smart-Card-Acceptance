{
    "name": "card-settlement-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "postinstall": "npm run build",
        "build": "cd backend && npm install --include=dev && npm rebuild sharp && chmod +x node_modules/.bin/prisma && npm run build && npm run prisma:generate",
        "start": "cd backend && npm start"
    },
    "engines": {
        "node": "20.x"
    }
}