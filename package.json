{
    "name": "card-settlement-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "postinstall": "npm run build",
        "build": "cd backend && npm install && npm run build && npx prisma generate",
        "start": "cd backend && npm start"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}